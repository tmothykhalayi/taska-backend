# Server
PORT=8000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=taska

# JWT (set at least one access secret key + one refresh secret key)
JWT_ACCESS_TOKEN_SECRET=
JWT_SECRET=
ACCESS_TOKEN_SECRET=
JWT_REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=

# Mail (optional depending on flow)
MAIL_HOST=
MAIL_PORT=587
MAIL_USER=
MAIL_PASSWORD=
